services:
  backend:
    build: .
    container_name: ai-backend
    env_file:
      - .env
    ports:
      - "8000:8000"

  frontend:
    build: ./client
    container_name: ai-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
